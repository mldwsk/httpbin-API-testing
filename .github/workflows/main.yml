name: Cypress tests
 on: push
   branches:
     - main
jobs:
 cypress-run:
  name: Cypress run
   runs-on: ubuntu-latest
   steps:
     - name:  Cypress run
        uses: cypress-io/github-action@v5